{{ form_start(form) }}
    
        <table class="uk-table uk-width-1-4 uk-table-justify" >  
            <tr>
                <th>NOM</th>
                <td align="right">{{ form_row(form.nom) }}</td>
            </tr> 
            <tr>
                <th>PRENOM</th>
                <td align="right">{{ form_row(form.prenom) }}</td>
            </tr> 
            <tr>
                <th>SEXE</th>
                <td align="right">{{ form_row(form.sexe) }}</td>
            </tr> 
            <tr>
                <th>Date de Naissance</th>
                <td align="right">{{ form_row(form.date_de_naissance) }}</td>
            </tr>
            <tr>
                <th>Nationalité</th>
                <td align="right">{{ form_row(form.nationalite) }}</td>
            </tr> 
        </table>
        <button class="uk-button uk-button-secondary ">{{ button_label|default('Save') }}</button>
         <a class="uk-button uk-button-secondary" href="{{ path('auteur_index') }}" uk-icon="list" uk-tooltip="back to list"></a>
    
{{ form_end(form) }}
