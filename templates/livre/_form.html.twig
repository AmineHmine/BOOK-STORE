{{ form_start(form) }}
        
        <table class="uk-table uk-width-1-4 uk-table-justify" >  
            <tr>
                <th>NOM</th>
                <td align="right">{{ form_row(form.isbn) }}</td>
            </tr> 
            <tr>
                <th>PRENOM</th>
                <td align="right">{{ form_row(form.titre) }}</td>
            </tr> 
            <tr>
                <th>SEXE</th>
                <td align="right">{{ form_row(form.nombre_pages) }}</td>
            </tr> 
            <tr>
                <th>Date de Naissance</th>
                <td align="right">{{ form_row(form.date_de_parution) }}</td>
            </tr>
            <tr>
                <th>Note / 10</th>
                <td align="right">{{ form_row(form.note) }}</td>
            </tr>
        </table>
        <button class="uk-button uk-button-secondary ">{{ button_label|default('Save') }}</button>
        <a class="uk-button uk-button-secondary" href="{{ path('livre_index') }}" uk-icon="list" uk-tooltip="back to list"></a>
    
{{ form_end(form) }}
